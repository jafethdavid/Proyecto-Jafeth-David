@model IS_161_Proyecto_Grupo2.Models.Categoria

<div class="card shadow col-md-8 mx-auto">
    <div class="card-header bg-warning text-white">Editar Categoría</div>
    <div class="card-body">
        @using (Html.BeginForm())
        {
            @Html.HiddenFor(m => m.IdCategoria)
            @Html.AntiForgeryToken()

            <div class="row">
                <div class="col-md-3 mb-3">
                    <label class="form-label">ID</label>
                    <input class="form-control" value="@Model.IdCategoria" readonly />
                </div>
                <div class="col-md-9 mb-3">
                    @Html.LabelFor(m => m.Nombre, new { @class = "form-label" })
                    @Html.TextBoxFor(m => m.Nombre, new { @class = "form-control" })
                    @Html.ValidationMessageFor(m => m.Nombre, "", new { @class = "text-danger" })
                </div>
            </div>

            <!-- ── NUEVO ────────────────────────────────────────── -->
            <div class="mb-3">
                @Html.LabelFor(m => m.Descripcion, new { @class = "form-label" })
                @Html.TextAreaFor(m => m.Descripcion, new { @class = "form-control", rows = 2, placeholder = "Descripción opcional" })
                @Html.ValidationMessageFor(m => m.Descripcion, "", new { @class = "text-danger" })
            </div>

            <!-- ── NUEVO ────────────────────────────────────────── -->
            <div class="mb-3">
                <div class="form-check form-switch">
                    @Html.CheckBoxFor(m => m.Estado, new { @class = "form-check-input" })
                    <label class="form-check-label">Activa</label>
                </div>
            </div>
            <!-- ────────────────────────────────────────────────── -->

            <hr />
            <div class="d-flex justify-content-between align-items-center mb-2">
                <h5>Subcategorías</h5>
                <a href="/Subcategoria/Create?idCategoria=@Model.IdCategoria" class="btn btn-sm btn-success">
                    + Agregar Subcategoría
                </a>
            </div>

            @if (Model.Subcategorias == null || !Model.Subcategorias.Any())
            {
                <div class="alert alert-info">No hay subcategorías</div>
            }
            else
            {
                <table class="table table-sm">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Nombre</th>
                            <th>Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var s in Model.Subcategorias)
                        {
                            <tr>
                                <td>@s.IdSubcategoria</td>
                                <td>@s.Nombre</td>
                                <td>
                                    <a href="/Subcategoria/Edit/@s.IdSubcategoria"
                                       class="btn btn-sm btn-warning me-1">Editar</a>
                                    <a href="/Subcategoria/Delete/@s.IdSubcategoria"
                                       class="btn btn-sm btn-danger"
                                       onclick="return confirm('¿Eliminar subcategoría?')">Eliminar</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            }

            <div class="text-end mt-3">
                <button type="submit" class="btn btn-warning">
                    <i class="bi bi-save"></i> Actualizar Categoría
                </button>
                <a href="/Categoria" class="btn btn-secondary">
                    <i class="bi bi-x-circle"></i> Volver
                </a>
            </div>
        }
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}